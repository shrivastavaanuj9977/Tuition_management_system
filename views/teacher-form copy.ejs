<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= teacher ? 'Edit Teacher' : 'Teacher Registration Form' %>
    </title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .container {
            width: 100%;
            max-width: 600px;
            background: #ffffff;
            padding: 40px;
            border-radius: 12px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
        }

        .header {
            display: flex;
            align-items: center;
            margin-bottom: 30px;
            gap: 12px;
        }

        .header i {
            font-size: 28px;
            color: #667eea;
        }

        h2 {
            color: #1a1a1a;
            font-size: 26px;
            font-weight: 600;
            margin: 0;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
        }

        .form-row.full {
            grid-template-columns: 1fr;
        }

        label {
            font-weight: 600;
            display: block;
            margin-bottom: 8px;
            color: #333;
            font-size: 14px;
        }

        input,
        select,
        textarea {
            width: 100%;
            padding: 12px;
            border: 1px solid #ddd;
            border-radius: 6px;
            font-size: 14px;
            font-family: inherit;
            transition: border-color 0.3s, box-shadow 0.3s;
            outline: none;
        }

        input:focus,
        select:focus,
        textarea:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }

        textarea {
            resize: vertical;
            min-height: 80px;
        }

        .button-group {
            display: flex;
            gap: 12px;
            margin-top: 30px;
        }

        button {
            flex: 1;
            padding: 12px;
            font-size: 16px;
            font-weight: 600;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            transition: all 0.3s;
        }

        .btn-submit {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }

        .btn-submit:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(102, 126, 234, 0.4);
        }

        .btn-cancel {
            background: #f0f0f0;
            color: #666;
        }

        .btn-cancel:hover {
            background: #e0e0e0;
        }

        .required {
            color: #e74c3c;
        }

        .form-info {
            background: #f0f7ff;
            padding: 12px;
            border-left: 4px solid #667eea;
            margin-bottom: 20px;
            border-radius: 4px;
            font-size: 13px;
            color: #555;
        }
    </style>
</head>

<body>

    <div class="container">
        <div class="header">
            <i class="fas fa-chalkboard-user"></i>
            <h2>
                <%= teacher ? 'Edit Teacher' : 'Add New Teacher' %>
            </h2>
        </div>

        <div class="form-info">
            <i class="fas fa-info-circle"></i> Please fill in all the required fields marked with <span
                class="required">*</span>
        </div>

        <form action="<%= teacher ? '/teachers/edit/' + teacher.id : '/teachers/add' %>" method="POST">

            <div class="form-row">
                <div class="form-group">
                    <label for="name">Full Name <span class="required">*</span></label>
                    <input type="text" id="name" name="name" placeholder="Enter full name"
                        value="<%= teacher ? teacher.name : '' %>" required>
                </div>

                <div class="form-group">
                    <label for="email">Email ID <span class="required">*</span></label>
                    <input type="email" id="email" name="email" placeholder="Enter email address"
                        value="<%= teacher ? teacher.email : '' %>" required>
                </div>
            </div>

            <div class="form-row">
                <div class="form-group">
                    <label for="phone">Mobile Number <span class="required">*</span></label>
                    <input type="text" id="phone" name="phone" placeholder="Enter mobile number"
                        value="<%= teacher ? teacher.phone : '' %>" required>
                </div>

                <div class="form-group">
                    <label for="dob">Date of Birth <span class="required">*</span></label>
                    <input type="date" id="dob" name="dob" value="<%= teacher ? teacher.dob : '' %>" required>
                </div>
            </div>

            <div class="form-row">
                <div class="form-group">
                    <label for="gender">Gender <span class="required">*</span></label>
                    <select id="gender" name="gender" required>
                        <option value="">-- Select Gender --</option>
                        <option <% if (teacher && teacher.gender==='Male' ) { %>selected<% } %>>Male</option>
                        <option <% if (teacher && teacher.gender==='Female' ) { %>selected<% } %>>Female</option>
                        <option <% if (teacher && teacher.gender==='Other' ) { %>selected<% } %>>Other</option>
                    </select>
                </div>

                <div class="form-group">
                    <label for="qualification">Qualification <span class="required">*</span></label>
                    <select id="qualification" name="qualification" required>
                        <option value="">-- Select Qualification --</option>
                        <option <% if (teacher && teacher.qualification==='B.A.' ) { %>selected<% } %>>B.A.</option>
                        <option <% if (teacher && teacher.qualification==='B.Sc.' ) { %>selected<% } %>>B.Sc.</option>
                        <option <% if (teacher && teacher.qualification==='B.Com.' ) { %>selected<% } %>>B.Com.</option>
                        <option <% if (teacher && teacher.qualification==='B.Tech.' ) { %>selected<% } %>>B.Tech.
                        </option>
                        <option <% if (teacher && teacher.qualification==='M.A.' ) { %>selected<% } %>>M.A.</option>
                        <option <% if (teacher && teacher.qualification==='M.Sc.' ) { %>selected<% } %>>M.Sc.</option>
                        <option <% if (teacher && teacher.qualification==='M.Tech.' ) { %>selected<% } %>>M.Tech.
                        </option>
                        <option <% if (teacher && teacher.qualification==='M.B.A.' ) { %>selected<% } %>>M.B.A.</option>
                        <option <% if (teacher && teacher.qualification==='B.Ed.' ) { %>selected<% } %>>B.Ed.</option>
                        <option <% if (teacher && teacher.qualification==='M.Ed.' ) { %>selected<% } %>>M.Ed.</option>
                    </select>
                </div>
            </div>

            <div class="form-row">
                <div class="form-group">
                    <label for="specialization">Specialization <span class="required">*</span></label>
                    <select id="specialization" name="specialization" required>
                        <option value="">-- Select Subject --</option>
                        <option <% if (teacher && teacher.specialization==='Mathematics' ) { %>selected<% } %>
                                >Mathematics</option>
                        <option <% if (teacher && teacher.specialization==='English' ) { %>selected<% } %>>English
                        </option>
                        <option <% if (teacher && teacher.specialization==='Science' ) { %>selected<% } %>>Science
                        </option>
                        <option <% if (teacher && teacher.specialization==='Social Studies' ) { %>selected<% } %>>Social
                                Studies</option>
                        <option <% if (teacher && teacher.specialization==='Hindi' ) { %>selected<% } %>>Hindi</option>
                        <option <% if (teacher && teacher.specialization==='Physics' ) { %>selected<% } %>>Physics
                        </option>
                        <option <% if (teacher && teacher.specialization==='Chemistry' ) { %>selected<% } %>>Chemistry
                        </option>
                        <option <% if (teacher && teacher.specialization==='Biology' ) { %>selected<% } %>>Biology
                        </option>
                        <option <% if (teacher && teacher.specialization==='History' ) { %>selected<% } %>>History
                        </option>
                        <option <% if (teacher && teacher.specialization==='Geography' ) { %>selected<% } %>>Geography
                        </option>
                        <option <% if (teacher && teacher.specialization==='Computer Science' ) { %>selected<% } %>
                                >Computer Science</option>
                    </select>
                </div>

                <div class="form-group">
                    <label for="experience">Years of Experience <span class="required">*</span></label>
                    <input type="number" id="experience" name="experience" placeholder="Enter years of experience"
                        value="<%= teacher ? teacher.experience : '' %>" min="0" required>
                </div>
            </div>

            <div class="form-row full">
                <div class="form-group">
                    <label for="address">Full Address</label>
                    <textarea id="address" name="address"
                        placeholder="Enter full address (optional)"><%= teacher ? teacher.address : '' %></textarea>
                </div>
            </div>

            <div class="form-row full">
                <div class="form-group">
                    <label for="bio">About Teacher</label>
                    <textarea id="bio" name="bio"
                        placeholder="Enter brief information about the teacher (optional)"><%= teacher ? teacher.bio : '' %></textarea>
                </div>
            </div>

            <div class="button-group">
                <button type="submit" class="btn-submit">
                    <i class="fas fa-save"></i>
                    <%= teacher ? 'Update' : 'Submit' %>
                </button>
                <button type="reset" class="btn-cancel">
                    <i class="fas fa-undo"></i> Reset
                </button>
                <a href="/teachers" style="flex: 1; text-decoration: none;">
                    <button type="button" class="btn-cancel" style="width: 100%;">
                        <i class="fas fa-arrow-left"></i> Back
                    </button>
                </a>
            </div>

        </form>
    </div>

</body>

</html>